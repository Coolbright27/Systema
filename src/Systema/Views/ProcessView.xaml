<UserControl x:Class="Systema.Views.ProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Systema.Core.Converters"
             Background="{StaticResource BgPrimaryBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="32,28,32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Process Management" Style="{StaticResource HeadingLarge}"/>
            <TextBlock Style="{StaticResource CaptionText}" Margin="0,4,0,0">
                <Run Text="{Binding Processes.Count, Mode=OneWay}"/>
                <Run Text=" background processes — sorted by memory usage (top 50)."/>
            </TextBlock>
        </StackPanel>

        <!-- Process List -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column Headers -->
                <Border Grid.Row="0" Background="{StaticResource BgSurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                        Padding="16,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="PID"      Style="{StaticResource CaptionText}" Grid.Column="0"/>
                        <TextBlock Text="NAME"     Style="{StaticResource CaptionText}" Grid.Column="1"/>
                        <TextBlock Text="RAM (MB)" Style="{StaticResource CaptionText}" Grid.Column="2" HorizontalAlignment="Right"/>
                        <TextBlock Text="PRIORITY" Style="{StaticResource CaptionText}" Grid.Column="3" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Loading overlay -->
                <Border Grid.Row="1"
                        Background="#880D0F14"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                        Panel.ZIndex="10">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="120" Height="4"
                                     Style="{StaticResource AccentProgressBar}"/>
                        <TextBlock Text="Loading..." Style="{StaticResource CaptionText}"
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Items -->
                <ListView Grid.Row="1" ItemsSource="{Binding Processes}"
                          Style="{StaticResource DarkListView}"
                          ItemContainerStyle="{StaticResource DarkListViewItem}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="16,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="110"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Pid}"
                                           Style="{StaticResource CaptionText}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding Name}"
                                           Style="{StaticResource BodyText}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding WorkingSetMb}"
                                           Style="{StaticResource BodyText}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                <Border Grid.Column="3" Background="{StaticResource BgSurfaceBrush}"
                                        CornerRadius="4" Padding="8,2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Priority}" Style="{StaticResource CaptionText}"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Bottom bar: status + refresh button -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"
                       Style="{StaticResource CaptionText}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="&#x21BB;  Refresh"
                    Style="{StaticResource GhostButton}" Padding="14,7"
                    Command="{Binding RefreshCommand}"/>
        </Grid>
    </Grid>
</UserControl>
